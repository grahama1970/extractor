# Provider Conversion Review — 2025-09-18

Scope: HTML, DOCX, PPTX, Spreadsheet, EPUB, RST, XML, Markdown providers and routing/CLI glue.

Summary
- Single CLI surface works across formats after extending `src/cli.py` valid extensions and adding XML mapping to the provider registry.
- Structured-all smoke passes for: HTML, DOCX (w/ PDF fallback), PPTX, XLSX, EPUB, RST, XML, MD.
- Stage 05 quality smoke passes (no fragmentation tokens detected on sample).

Findings & Small Fixes Applied
- PPTX: Removed unused import `MSO_THEME_COLOR`.
- Spreadsheet: Guarded `ws._images` usage (`getattr` + try/except) to avoid crashes on older openpyxl.
- Registry: Added `xml -> XMLProvider` mapping.
- CLI: Allowed structured formats by adding extensions: `.xlsx, .xlsm, .xls, .ods, .rst, .md`.
- Providers: Fixed example docstrings to reference the right class names (no-ops for runtime).

Notable Behavior
- DOCX fallback to accurate PDF pipeline is triggered for the sample; works offline with deterministic toggles.
- XML sample required fallback root wrapping; provider handles it and emits Stage 07/10.

Follow-ups (recommended)
- Meta parity smoke currently marks many formats as "missing stage10" despite stage outputs being created; inspect its stage10 detection or working directory assumptions.
- Consider normalizing provider image handling (e.g., Spreadsheet image MIME detection) and explicitly documenting fields used by Stage 10.
- Evaluate if EPUB BeautifulSoup parser warning should be silenced or switched to XML parser per hint (optional).

Artifacts of Interest
- Structured-all outputs: `data/results/cli_smokes/structured_all/<fmt>/<stem>/07_*/json_output/07_reflowed.json` and corresponding Stage 10.
- Meta parity summary (may report false negatives for missing stage10): `scripts/artifacts/meta_cli_parity_summary.json`.
- Stage 05 quality: `scripts/artifacts/stage05_strategy_quality.json`.
