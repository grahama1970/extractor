# Contract Loop Task Index (Example)

Each task in the contract loop references a JSON contract file. This example
shows the minimal format the orchestrator parses.

## Task 00 — Example Contract

Contract file: `contract_task_example.json`
