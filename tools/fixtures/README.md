# Fixtures

This directory holds **local deterministic fixtures** used by sanity scripts and gates.

- `camelot_fixture.pdf` should be a small PDF containing at least one clear table on page 1.

Why:
- Sanity scripts should avoid network and external dependencies.
- A fixture makes Camelot checks reproducible.

Add your fixture PDF at:
- `fixtures/camelot_fixture.pdf`
