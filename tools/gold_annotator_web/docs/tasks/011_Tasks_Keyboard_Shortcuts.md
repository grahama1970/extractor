# 011 — Keyboard Shortcuts

Goal: verify shortcut keys for quick labeling and actions.

- [ ] With a box selected, press the shortcut keys for types (e.g., `f` for field, `t` for table) if supported.
- [ ] Screenshot: `docs/screenshots/011_shortcuts/01_type_shortcut.png`.
- [ ] Press Save shortcut (e.g., `Ctrl/Cmd+S`) if supported; confirm save.
- [ ] Screenshot: `docs/screenshots/011_shortcuts/02_save_shortcut.png`.
- [ ] Use navigation shortcuts (e.g., `[` and `]` for pages) if implemented.
- [ ] Screenshot: `docs/screenshots/011_shortcuts/03_nav_shortcuts.png`.

Notes

- Check `app/page.tsx` for exact keybindings if unsure.

