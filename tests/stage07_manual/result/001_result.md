```json
{
  "reflowed_text": "### 4.1.5.4. BHT (Branch History Table) Submodule\n\nThe Branch History Table (BHT) is implemented as a memory composed of **BHTDepth** configuration parameter entries. The lower address bits of the virtual address point to the corresponding memory entry.\n\nWhen a branch instruction is resolved by the **EX_STAGE** module, the branch PC and the taken (or not taken) status information are stored in the BHT.\n\nThe BHT is a table of **two-bit saturating counters** indexed by the virtual address of the currently fetched instruction from the **CACHE**. It indicates whether the current branch request should be taken or not. The counter is updated by successive instruction executions, as illustrated below:\n\n![Two-bit saturating counter state diagram](figure1.png)\n\nWhen a branch instruction is pre-decoded by the **instr_scan** submodule, the BHT checks whether the PC address is present and provides a taken/not-taken prediction.\n\nThe BHT is never flushed.\n\n#### Signal Interface\n\n| Signal           | IO   | Description                           | Connection | Type                                                                 |\n|------------------|------|---------------------------------------|------------|----------------------------------------------------------------------|\n| clk_i            | in   | Subsystem clock                       | SUBSYSTEM  | logic                                                               |\n| rst_ni           | in   | Asynchronous reset (active low)       | SUBSYSTEM  | logic                                                               |\n| vpc_i            | in   | Virtual PC                            | CACHE      | logic[CVA6Cfg.VLEN-1:0]                                             |\n| bht_update_i     | in   | Update BHT with resolved address      | EXECUTE    | bht_update_t                                                        |\n| bht_prediction_o | out  | Prediction from BHT                   | FRONT END  | ariane_pkg::bht_prediction_t[CVA6Cfg.INSTR_PER_FETCH-1:0]           |\n\n#### Static Port Configuration\n\nDue to cv32a65x configuration, some ports are tied to static values and are not listed in the table:\n\n- For any HW configuration:\n  - `flush_bp_i` input is tied to 0\n\n- As DebugEn = False:\n  - `debug_mode_i` input is tied to 0",
  "ocr_corrections": {
    "Descripti\non": "Description",
    "connexi\non": "Connection",
    "Subsyste\nm": "Subsystem",
    "Asynchro\nnous": "Asynchronous",
    "bht_updat\ne_i": "bht_update_i",
    "bht_predi\nction_o": "bht_prediction_o"
  },
  "improvements_made": "Fixed broken words and hyphenations, corrected OCR line breaks in table headers and entries, consolidated duplicated static configuration notes, and reformatted the section into clean Markdown with a structured table and figure reference.",
  "summary": "This section describes the Branch History Table (BHT) submodule, which uses two-bit saturating counters to predict branch outcomes. It details how the BHT is indexed, updated, and used during instruction fetch and pre-decode, provides the relevant signal interface table, and clarifies static port configurations."
}
```
