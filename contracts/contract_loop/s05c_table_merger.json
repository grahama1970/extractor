{
  "task_id": "s05c_table_merger",
  "title": "Stage 05c table merger contract",
  "prompt": "Ensure Stage 05c (s05c_table_merger) meets the pipeline contract. Run uv run scripts/smokes/smoke_stage05c_table_merger.py and fix any failures. Do not change unrelated stages. Return JSON with keys ok, summary, artifacts.",
  "max_iters": 3,
  "deterministic_checks": {
    "commands": [
      {
        "name": "s05c_smoke",
        "cmd": "uv run scripts/smokes/smoke_stage05c_table_merger.py"
      }
    ],
    "files_exist": [
      "src/extractor/pipeline/steps/s05c_table_merger.py",
      "tools/contract_loop/adapters/extractor/docs/SANITY_MATRIX.md"
    ],
    "files_contain": [
      {
        "path": "tools/contract_loop/adapters/extractor/docs/SANITY_MATRIX.md",
        "contains": [
          "s05c_table_merger.py"
        ]
      }
    ]
  },
  "agent": {
    "output_schema": "tools/contract_loop/judges/agent_task.schema.json"
  },
  "llm_gate": {
    "schema": "tools/contract_loop/judges/contract_gate.schema.json",
    "prompt": "You are the contract auditor for Stage 05c. Approve only if the smoke script passed and artifacts match the contract. If anything is missing, return ok=false with next_actions."
  }
}
